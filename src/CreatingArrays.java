public class CreatingArrays {
    public static void main(String[] args) {
        // int[] counts = new int[1];
        // counts[0] = 7;
        // counts[1] = counts[0] * 2;
        // counts[2]++;
        // counts[3] -= 60;
    }
}
